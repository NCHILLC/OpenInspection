{
  "app/components/agent/AgentRepairInspectionBlock.tsx::sendEmail::emailFetcher.submit(fd, { method: \"post\" })": "FormData payload: the repair-share email posts a FormData, which useGuardedSubmit's Record<string,string> signature cannot carry.",
  "app/components/agent/AgentRepairInspectionBlock.tsx::share::shareFetcher.submit(fd, { method: \"post\" })": "FormData payload: useGuardedSubmit's submit accepts only Record<string,string>, and this share call posts a FormData built field by field. Forcing it through the hook would change the request body.",
  "app/components/agreements/RequestDetail.tsx::RequestDetail::loadFetcher.submit({ intent: \"load-signers\", requestId }, post)": "Read, not a mutation: the load-signers intent only selects signer rows, and it is fired from a useEffect keyed on requestId rather than by a control.",
  "app/components/agreements/RequestDetail.tsx::RequestDetail::loadFetcher.submit({ intent: \"load-signers\", requestId }, post) #2": "Read, not a mutation: the same load-signers select, re-fired from an effect after a reminder lands. Effect-driven, so there is no control to disable and no double click to contain.",
  "app/components/agreements/RequestDetail.tsx::onCopyLink::copyFetcher.submit({ intent: \"copy-link\", requestId, signerId }, post)": "Read, not a mutation: copy-link resolves the persistent signing URL for an existing envelope; the promise wrapper polls the same fetcher for the answer, which a refusal-returning submit would break.",
  "app/components/booking/useBookingFormState.ts::submitAgentHold::agentFetcher.submit(fd, { method: \"post\" })": "FormData payload: the agent hold builds a FormData with repeated serviceId entries, which a Record<string,string> cannot express at all.",
  "app/components/checkout/SignCard.tsx::submitSignature::fetcher.submit(fd, { method: \"post\" })": "FormData payload: the signature ride-along posts a base64 image plus on-behalf fields as FormData.",
  "app/components/editor/InspectionSettingsSheet.tsx::handleSave::coverFetcher.submit(fd, { method: \"post\", encType: \"multipart/form-data\" })": "FormData payload: the cover cropper posts the cropped image as a multipart File.",
  "app/components/editor/InspectionSettingsSheet.tsx::selectCover::coverFetcher.submit({ intent: \"set-cover\", coverPhotoId: \"\" }, { method: \"post\" })": "Shared fetcher: coverFetcher also carries the two multipart cover uploads in this file (upload-cover, crop-cover), which cannot go through useGuardedSubmit. Converting only this call would split one fetcher's traffic across two mechanisms and change which submissions abort each other.",
  "app/components/editor/InspectionSettingsSheet.tsx::uploadCover::coverFetcher.submit(fd, { method: \"post\", encType: \"multipart/form-data\" })": "FormData payload: direct cover upload posts a multipart body with a File.",
  "app/components/editor/PropertyInfoForm.tsx::runAutofill::autofillFetcher.submit({ intent: \"autofill-property-facts\", addressString }, { method: \"post\" })": "Read, not a mutation: autofill-property-facts resolves public-records data for the form to display; the durable write happens later through save-property-facts.",
  "app/components/email-template/EmailPreview.tsx::EmailPreview::fetcher.submit(fd, { method: \"post\" })": "Effect-driven, and a preview rather than a write: fired from a 350ms debounce inside a useEffect on every keystroke. The hook's mount-once key would make every debounce tick after the first read as a replay.",
  "app/components/inspection/AddPersonModal.tsx::handleSearchChange::searchFetcher.submit({ intent: \"search-contacts\", search: value.trim() }, { method: \"post\" })": "Read, not a mutation: search-contacts only selects contact rows, and it is fired from a 300ms debounce timer rather than by a control.",
  "app/components/inspector-portal/OrderDetailsCard.tsx::onReferrerInput::searchFetcher.submit({ intent: \"search-contacts\", search: value.trim() }, { method: \"post\" })": "Read, not a mutation: search-contacts only selects contact rows, fired from a 250ms debounce timer rather than by a control.",
  "app/components/media-studio/PosterPicker.tsx::setPoster::fetcher.submit( JSON.stringify({ streamUid, posterPct: f.pct }), { method: \"POST\", action: `/api/inspections/${inspectio": "Not a Record<string,string> payload: this posts a JSON string with encType application/json, a shape useGuardedSubmit's signature cannot express without changing the request body.",
  "app/components/modals/EditMemberDrawer.tsx::submit::fetcher.submit(fd, { method: \"POST\", action: \"/resources/team-members\" })": "FormData payload: the member update posts a FormData carrying serialized permission overrides.",
  "app/components/modals/InviteSeatDrawer.tsx::submitPermanent::inviteFetcher.submit(fd, { method: \"POST\", action: \"/resources/team-members\" })": "FormData payload: the seat invite posts a FormData carrying serialized permission overrides.",
  "app/components/portal/sections/AgreementSection.tsx::submitDecline::declineFetcher.submit(fd, { method: \"post\", action: actionPath })": "FormData payload: the agreement decline posts a FormData carrying the optional reason.",
  "app/components/portal/sections/AgreementSection.tsx::submitSignature::signFetcher.submit(fd, { method: \"post\", action: actionPath })": "FormData payload: the agreement signature posts a base64 image plus on-behalf fields as FormData.",
  "app/components/portal/sections/RepairBuilderSection.tsx::RepairBuilderUI::introFetcher.submit(fd, { method: \"post\", action: actionPath })": "FormData payload: the repair-builder intro save posts a FormData carrying the portal token.",
  "app/components/portal/sections/RepairBuilderSection.tsx::sendEmail::emailFetcher.submit(fd, { method: \"post\", action: actionPath })": "FormData payload: the repair-builder email posts a FormData carrying the portal token.",
  "app/components/portal/sections/repair/useRepairOpQueue.ts::useRepairOpQueue::createFetcher.submit(createFd, { method: \"post\", action: actionPath })": "Own de-dup, and a FormData payload: create-list is fired at most once per queue burst by the creatingRef guard in this hook, not by a control.",
  "app/components/portal/sections/repair/useRepairOpQueue.ts::useRepairOpQueue::mutationFetcher.submit(next, { method: \"post\", action: actionPath })": "Queue drain, not a control: this replays FormData ops the hook has already queued, one after another, and the hook owns its own in-flight bookkeeping. A guard that refuses a submit while one is in flight would stall the queue permanently.",
  "app/components/portal/sections/report/WordExportButton.tsx::WordExportButton::fetcher.submit({ intent: \"export-word-status\", exportId }, { method: \"POST\" })": "Poll, not a control: fired from a setInterval inside a useEffect while the export is queued or building, and already skipped when the fetcher is not idle.",
  "app/components/portal/sections/report/WordExportButton.tsx::enqueue::fetcher.submit({ intent: \"export-word-enqueue\" }, { method: \"POST\" })": "Shared fetcher: the same fetcher carries the status poll above, which cannot be guarded. Converting only the enqueue call would split one fetcher's traffic across two mechanisms.",
  "app/components/settings/ProfilePhotoCard.tsx::closeCropper::fetcher.submit(fd, { method: \"POST\", encType: \"multipart/form-data\" })": "FormData payload: the avatar cropper posts the cropped image as a multipart File.",
  "app/components/settings/SignatureCards.tsx::submit::fetcher.submit(fd, { method: \"post\" })": "FormData payload: the drawn signature is posted as a FormData carrying the base64 image.",
  "app/hooks/useContactSearch.ts::onQueryChange::fetcher.submit({ intent, search: q }, { method: \"post\", action: \"/inspections\" })": "Read, not a mutation: this shared hook only runs a contact search, and it is fired from a 300ms debounce timer rather than by a control.",
  "app/hooks/useInspectionPrefs.ts::useInspectionPrefs::patchFetcher.submit( { patch: JSON.stringify(pendingDelta.current) }, { method: 'post', action: '/resources/inspection-p": "Coalesced burst, not one control press: preference deltas are merged into pendingDelta and re-submitted on every edit in a burst. A guard that refuses while one PATCH is in flight would drop the later deltas the merge depends on.",
  "app/hooks/useStructureEdit.ts::useStructureEdit::structureFetcher.submit( { intent: \"restructure\", snapshot: JSON.stringify(next), collab: collabEditing ? \"1\" : \"0\", }, ": "Chained structural edits: the hook deliberately advances its snapshot ref optimistically so a second structural op composes on top of an in-flight one. React Router's abort-and-resubmit is load-bearing here; refusing the second op instead would silently drop the edit.",
  "app/hooks/useStructureEdit.ts::useStructureEdit::structureFetcher.submit(fields, { method: \"post\" })": "Shares structureFetcher with applyStructure above, whose chained-edit behaviour cannot be guarded. Converting only this call would split one fetcher's traffic across two mechanisms.",
  "app/routes/inspection-edit.tsx::InspectionEditPage::fetcher.submit( { intent: 'set-item-attribute', itemId, attributeId, value: JSON.stringify(value), }, { method: 'POST' }": "Rapid per-attribute writes: set-item-attribute fires as the inspector edits item attributes, so successive writes for different attributes are expected inside one in-flight window. A guard would refuse the second attribute's value.",
  "app/routes/inspection-edit.tsx::InspectionEditPage::propertyFactsFetcher.submit({ intent: \"save-property-facts\", payload: JSON.stringify(facts) }, { method: \"POST\" })": "Last-write-wins by design: onCommit hands a FULL snapshot each time, so React Router cancelling the previous submission loses nothing. A guard would refuse the later, strictly-newer PATCH instead of cancelling the older one, which inverts that guarantee.",
  "app/routes/inspection-edit.tsx::InspectionEditPage::scopeFetcher.submit({ intent: \"load-scope\", scope }, { method: \"POST\" })": "Read, not a mutation: load-scope fetches the findings slice for a scope the editor has not loaded yet.",
  "app/routes/inspection-edit.tsx::handler::coverFetcher.submit( { intent: \"set-cover\", coverPhotoId: isCover ? \"\" : photoStudioKey }, { method: \"post\" }, )": "Shared fetcher: coverFetcher also carries the annotate and crop-cover multipart uploads in this file. Converting only this call would split one fetcher's traffic across two mechanisms.",
  "app/routes/inspection-edit.tsx::handler::coverFetcher.submit(fd, { method: \"post\", encType: \"multipart/form-data\" })": "FormData payload: the photo annotation posts the rendered image as a multipart File.",
  "app/routes/inspection-edit.tsx::run::coverFetcher.submit(fd, { method: \"post\", encType: \"multipart/form-data\" })": "FormData payload: the gallery cover cropper posts the cropped image as a multipart File.",
  "app/routes/inspection-edit/useEditorPhotoUpload.ts::useEditorPhotoUpload::uploadFetcher.submit(formData, { method: \"post\", encType: \"multipart/form-data\" })": "FormData payload: the photo upload posts a multipart body with one or more Files.",
  "app/routes/inspection-edit/useEditorPhotoUpload.ts::useEditorPhotoUpload::uploadFetcher.submit(formData, { method: \"post\", encType: \"multipart/form-data\" }) #2": "FormData payload: the burst-camera upload posts a multipart body with the captured frames.",
  "app/routes/inspections.tsx::batchDelete::fetcher.submit({ intent: \"delete\", id }, { method: \"post\" })": "N submissions from one press: batchDelete loops over every selected id and submits once per id. The hook refuses a second submit while one is in flight, so converting would delete only the first inspection and silently drop the rest.",
  "app/routes/inspections.tsx::transitionStatus::fetcher.submit({ intent: \"status\", id, status }, { method: \"post\" })": "Shares the batch-delete fetcher above, which cannot be guarded without dropping submissions.",
  "app/routes/inspector-portal.tsx::submitPreSignature::fetcher.submit( { intent: \"inspector-sign\", envelopeId: preSigningId ?? \"\", signatureBase64: dataUri }, { method: \"post\"": "Fetcher owned by useModalFetcher, same as the send-agreement call above.",
  "app/routes/inspector-portal.tsx::submitSendAgreement::fetcher.submit( { intent: \"send-agreement\", agreementId: payload.agreementId, completionPolicy: payload.completionPolicy": "Fetcher owned by useModalFetcher: the modal hook creates and tracks the fetcher and reads its settle state to close itself. Routing this through useGuardedSubmit means changing that hook's contract, which is a separate change from this mechanical pass.",
  "app/routes/settings-contractor-types.tsx::requestDelete::preflightFetcher.submit({ intent: \"preflight\", id: t.id }, { method: \"POST\" })": "Read, not a mutation: the preflight intent counts the contractor types that would be orphaned so the confirm dialog can say so. The delete is a separate intent.",
  "app/routes/settings-profile.tsx::onCredReorder::credFetcher.submit({ intent: \"credential-reorder\", ids: orderedIds.join(\",\") }, { method: \"post\" })": "Shared credFetcher, and half its traffic is blur-driven: one fetcher carries credential-add, credential-update (fired on field blur), credential-delete and credential-reorder. React Router aborts the in-flight submission when the same fetcher re-submits; useGuardedSubmit would REFUSE the newer one instead, so two quick blurs would drop the second edit rather than the first.",
  "app/routes/settings-profile.tsx::onCredUpdate::credFetcher.submit( { intent: \"credential-update\", id, ...(patch.label !== undefined ? { label: patch.label } : {}), ...": "Shared credFetcher, and half its traffic is blur-driven: one fetcher carries credential-add, credential-update (fired on field blur), credential-delete and credential-reorder. React Router aborts the in-flight submission when the same fetcher re-submits; useGuardedSubmit would REFUSE the newer one instead, so two quick blurs would drop the second edit rather than the first.",
  "app/routes/settings-profile.tsx::onCredUpdate::credFetcher.submit({ intent: \"credential-delete\", id }, { method: \"post\" })": "Shared credFetcher, and half its traffic is blur-driven: one fetcher carries credential-add, credential-update (fired on field blur), credential-delete and credential-reorder. React Router aborts the in-flight submission when the same fetcher re-submits; useGuardedSubmit would REFUSE the newer one instead, so two quick blurs would drop the second edit rather than the first.",
  "app/routes/settings-profile.tsx::onCredUpload::credImageFetcher.submit(f, { method: \"post\", encType: \"multipart/form-data\" })": "FormData payload: the credential image is posted as a multipart File.",
  "app/routes/settings-profile.tsx::onValidate::credFetcher.submit({ intent: \"credential-add\" }, { method: \"post\" })": "Shared credFetcher, and half its traffic is blur-driven: one fetcher carries credential-add, credential-update (fired on field blur), credential-delete and credential-reorder. React Router aborts the in-flight submission when the same fetcher re-submits; useGuardedSubmit would REFUSE the newer one instead, so two quick blurs would drop the second edit rather than the first.",
  "app/components/settings/LogoField.tsx::LogoField::fetcher.submit(fd, { method: \"POST\", encType: \"multipart/form-data\" })": "FormData payload: the company logo is posted as a multipart File."
}
